export enum HorizontalContentAlign {
    left = 'left',
    center = 'center',
    right = 'right'
}

export enum VericalContentAlign {
    top = 'top',
    center = 'center',
    bottom = 'bottom'
}
